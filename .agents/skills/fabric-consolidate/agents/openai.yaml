interface:
  display_name: "Consolidate Fabric Direction"
  short_description: "Curate direction after work completes"
  default_prompt: "Use $fabric-consolidate to classify the direction left by this completed PR."
policy:
  allow_implicit_invocation: true
